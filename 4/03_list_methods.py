a = [1, 52, 63, 35, 78, 89, 87, 64]
# a.sort()# sorts the list
# a.reverse()# reverses the list
# a.append(0)# adds any datatype at the end of list
# a.insert(1,"A")# adds the datatype to the desired index
#a.pop(1) #removes the datatype from the desired index
a.remove(78)#removes the desired datatype from the list
print(a)